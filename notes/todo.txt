- wireframe mesh with separated wireframe shader (no need for normals or UV coord);
    - shader manager with different shader for different mesh
- crosshair line path locking system: visual aid for when a dynamic/shootable object is in shooting path;
- debug crosshair line path: real path and calculated path are different (try integrate with delta t as close as in game timestep)
- add interactable object: 
    [x] if 
            1 - inside interaction collision box 
            2 - interact button is pressed (same as shooting button)
            3 - direction is towards interactable object (or mouse-world ray is colliding with interaction box)
        then: interaction should begin
    [x] add selection visual aid like locked enemy
        - interactable info component { bool selected; ... }
        - single reset system with all reset functions (enemy locked, interactable select, etc...)
    [ ] interactabe shape information inside dynamic AABB tree node data
    [x] different type of interactables:
        - only-player interactable
        - automatic / clickable
- allow crosshair to have different z coord respect to character:
    idea 1: crosshair is mouse-world ray intersection with world collision shapes;
    idea 2: have a separated ground-crosshair collision set of planes, all of them pointing "up" (woth some tolerance, 
            for example stair inclined plane), crosshair is intersection of mouse-world ray and this set of planes;
- make world objects transparent when player is behind them: all world objects intersecting with character-camera ray become transparent;
    [x] add alpha to render info component and to fragment shader
    [ ] correct stair-ray intersection
    [ ] sort transparent objects by distance from camera and render from furthest to closest
- different implementation for dynamic and static AABB tree
    [x] optimize static AABB tree construction
        [ ] add suffix/prefix accumulators
    - dynamic AABB tree
        [x] dynamic memory implementation: 
            [ ] rebalancing tree based on height
        [x] add projectile to dynamic AABB tree
            [ ] enlarge in the shooting direction
    [x] ad-hoc data structure for queryOverlap result
- improve mesh manager:
    [ ] stop loading mesh in dynamic memory
    [x] have dictionary to store meshes
- new collision shapes:
    [x] AACylinder   { Real3 pos; Real rad; Real height; Axis axis; }     (axis in {X, Y, Z})
    [x] AAHemisphere { Real center; Real rad; Axis axis; bool positive; }
    [x] Sphere       { Real center; Real rad; }

    [ ] mtv in AABB-Cylinder collision 
        - if collision found: mtv is minimum between penetration in the aligned axis and circle-rect collision (check etricky weird cases)

[ ] unify shape update: now different handling for physics bodies and interactable infos

[ ] use quaternions for orientation

[ ] interaction-projectile coupling 

[x] projectile do not integrate over time: it has time counter (time passed from shooting), init shooting state (velocity, position)

[x] death-lifetime system: now duplicate code for interactable collision shape, try to unify the systems 

[ ] handle collision with potion and dynamic object

[ ] destructable object: when projectile hit them they get destroied
    - in physics body add bool destructable;
    - they must be static object in dynamic world aabb (because we remove them from aabb tree when destroyed);
    - check must be when projectile hit static object in dynamic world aabb tree and object is destructable;

[ ] crosshair trajectory is larger at the end to indicate shooting error

- aiming
    [ ] mouse pointer ray intersection with static world is the target
        [ ] also add dynamic world aabb tree static world to this calculation
    [ ] "X" pointer when target is unreachable
    [ ] "O" or "X" target pointer aligned to normal contact
        - in ray collision result add Real3 intersection_normal, and calculate it in ray collision with collision shapes

[x] door:
    - bool locked  (can not be opened)
    - bool open    (is open right now, can not be true if locked is true)
    - interaction shape:
        - every one can open it;
        - not clickable;
        - when interacted change static collision shape
    - physics body:
        - no physics
        - static type
        - in dynamic world 
    struct DoorInfo
    {
        bool locked;
        bool open;
        CollisionShape closed_shape;
        CollisionShape open_shape;
    } 
